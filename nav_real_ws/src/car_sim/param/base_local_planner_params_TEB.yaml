TebLocalPlannerROS:

  # EPRobot V2 阿克曼底盘, F1 比赛参数（craic 原调参 + 关闭 homotopy，见 QUICKSTART §7）
  odom_topic: odom
  map_frame: map

  # ====== 运动学约束 ======
  max_vel_x: 3.5
  max_vel_x_backwards: 1.0
  max_vel_y: 0.0
  max_vel_theta: 2.3
  acc_lim_x: 1.2
  acc_lim_y: 0.0
  acc_lim_theta: 1.0
  min_turning_radius: 0.52
  wheelbase: 0.17
  cmd_angle_instead_rotvel: false

  # ====== 轨迹生成 ======
  teb_autosize: true
  dt_ref: 0.3
  dt_hysteresis: 0.1
  min_samples: 3
  global_plan_overwrite_orientation: true
  # 从全局路径抽 via-point 的间距（越小局部轨迹越贴全局线）
  global_plan_viapoint_sep: 0.25
  max_global_plan_lookahead_dist: 2.0
  global_plan_prune_distance: 0.6
  # 允许轨迹以倒车段起步/衔接，避免「目标在身后却非要掉头」
  allow_init_with_backwards_motion: true

  # ====== 目标容差 ======
  xy_goal_tolerance: 0.12
  yaw_goal_tolerance: 0.1
  free_goal_vel: false

  # ====== 机器人外形（与 costmap_common_params.yaml 一致）======
  footprint_model:
    type: "polygon"
    vertices: [[-0.145, -0.105], [0.145, -0.105], [0.145, 0.105], [-0.145, 0.105]]

  # ====== 避障参数 (teb调整-F1参数.txt) ======
  min_obstacle_dist: 0.04
  inflation_dist: 0.05
  include_costmap_obstacles: true
  costmap_obstacles_behind_robot_dist: 1.0
  obstacle_poses_affected: 15

  # ====== 优化权重 ======
  no_inner_iterations: 5
  no_outer_iterations: 4
  weight_optimaltime: 5.0
  weight_max_vel_x: 2.0
  weight_max_vel_theta: 1.0
  weight_acc_lim_x: 1.0
  weight_acc_lim_theta: 1.0
  weight_kinematics_nh: 1000.0
  # 原 800 过强惩罚倒车；降低后短距离目标在身后时可优先后退
  weight_kinematics_forward_drive: 80.0
  weight_kinematics_turning_radius: 18.0
  weight_obstacle: 50.0
  # 增大后局部轨迹更贴近全局路径上的 via-point
  weight_viapoint: 20.0
  weight_shortest_path: 2.0

  # ====== 根因修复：须显式关闭，否则 TEB 默认开启多拓扑，Pi 上 /cmd_vel 仅 ~1Hz ======
  enable_homotopy_class_planning: false
